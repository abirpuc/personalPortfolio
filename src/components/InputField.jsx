import React from 'react'

export default function InputField() {
    // {error,handleOnchange, type, name, placeholder}
    return (
        <div className='w-full'>
            {/* <input className='inputField  p-2 w-full text-md' type={type} name={name} placeholder={placeholder} onChange={handleOnchange} />
            {
                error.name && <p className='text-red-700'>{error.name}</p>
            } */}
        </div>
    )
}
